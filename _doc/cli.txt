

utiliser une cli chez ovh en mutualise est un parcours du combattant.
J'ai supprimé la vérification dans moodle pour pouvoir effectuer la mise à jour via l'interface web.
Il faudrait que je vois ce que celà donne en curl à partir de notre serveur.

/* update users */
lib/setup.php delete checking cli demand
line 173 / /*if (defined('WEB_CRON_EMULATED_CLI')) { 
http://moodle.formations-epfc.be/auth/db/cli/sync_users.php